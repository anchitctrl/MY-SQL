SELECT 
product_id,
name,
unit_price AS 'original factor',
(unit_price*10)+100 AS factor
FROM products


-- we can write down the names of required attributes in multiple lines also
-- use AS clause to give the name of column
-- if you need space in the changed column name put it inside either sigle or double quotes
